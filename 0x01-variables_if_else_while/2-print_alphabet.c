#include <stdio.h>
/**
 * main - Entry
 * Return: Always 0
 */

int main(void)
{
	char alpha;
	/*for loop*/
	for (alpha = 'a'; alpha <= 'z'; alpha++)
		putchar (alpha);
	putchar ('\n');
	return (0);
}
